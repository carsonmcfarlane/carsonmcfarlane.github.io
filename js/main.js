// Shrink header on scroll
window.addEventListener('scroll', () => {
  console.log('Scroll Y:', window.scrollY); // Debug scroll position
  //header.classList.toggle('scrolled', window.scrollY > 50); // >50px to start shrinking
});